<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
  <%= favicon_link_tag 'elit-logo-2.png', type: 'image/png' %>
  <%= favicon_link_tag 'elit-logo-2.png', rel: 'apple-touch-icon', type: 'image/png' %>
  <meta name="theme-color" content="#ff4a46">
  <title>ЭЛИТ - Ювелирные салоны в Железногорске. Купить ювелирные изделия в Железногорске</title>
  <meta name="keywords" content="элит железногорск магазин салон ювелирный купить серьги кольцо браслет сайт">
  <meta name="description" content="ЭЛИТ - ювелирные салоны в г. Железногорске, Курская область ✔ Широкий ассортимент ювелирных изделий ✔ Серьги, кольца, браслеты. Скупка и обмен.">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0">
  <%= csrf_meta_tags %>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <%= stylesheet_link_tag 'store/store', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>

<body class="nav-on-left">

<%= render 'layouts/header' %>

<main class="cd-main-content" style="padding:0;">

  <%= yield %>

</main>

<div class="cd-overlay"></div>

<%= render 'layouts/renders/basket' %>

<%= render 'layouts/renders/footer' %>


<%= javascript_include_tag 'store/store' %>
<%= javascript_include_tag 'store/main_page/main_page'%>

<script>
    $(document).ready(function() {

        $('#main-slider').owlCarousel({
            items:1,
            loop:true,
            nav:true,
            margin:0,
            URLhashListener:true,
            autoplay: true,
            autoplayTimeout: 4000,
            autoplayHoverPause: true,
            startPosition: 'URLHash'
        });

        $('#sale_slider-1').owlCarousel({
            loop:true,
            nav:true,
            margin:0,
            responsiveClass: true,
            responsive: {
                0: { items:2 },
                600: { items:3 },
                800: { items:3 },
                1280: { items:4 }
            }
        });

        $('#sale_slider-2').owlCarousel({
            loop:true,
            nav:true,
            margin:0,
            responsiveClass: true,
            responsive: {
                0: { items:2 },
                600: { items:3 },
                800: { items:3 },
                1280: { items:4 }
            }
        });

        $('#sale_slider-3').owlCarousel({
            loop:true,
            nav:true,
            margin:0,
            responsiveClass: true,
            responsive: {
                0: { items:2 },
                600: { items:3 },
                800: { items:3 },
                1280: { items:4 }
            }
        });

        $('#logo-slider').owlCarousel({
            autoplay: true,
            autoplayTimeout: 2000,
            loop:true,
            nav:false,
            dots: false,
            margin:0,
            responsiveClass: true,
            responsive: {
                0: { items:2 },
                700: { items:3 },
                800: { items:3 },
                1200: { items:4 }
            }
        });
    });

</script>

<script>
    window.sr = ScrollReveal({
        duration: 3000,

        origin: 'bottom',
        reset: false
    });
    sr.reveal('.elit-reveal', {
        viewFactor: 0.1,
        duration: 1000,
        reset: false
    });

</script>

</body>
</html>