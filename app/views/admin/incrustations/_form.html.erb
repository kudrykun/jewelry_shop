<%= form_for([:admin,incrustation]) do |f| %>
  <% if incrustation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(incrustation.errors.count, "error") %> prohibited this incrustation from being saved:</h2>

      <ul>
      <% incrustation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, 'Название' %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
