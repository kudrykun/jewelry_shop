<%= form_for([:admin,sale_size]) do |f| %>
  <% if sale_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale_size.errors.count, "error") %> prohibited this sale_size from being saved:</h2>

      <ul>
      <% sale_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="row">
      <div class="col-lg-6">
        <div class="form-group">
          <%= f.label :sale_percent, 'Размер скидки' %>
          <%= f.text_field :sale_percent, :placeholder => 'Введите скидку  ', class: 'form-control' %>
        </div>

        <div class="actions">
          <%= f.submit 'Применить', class: 'btn btn-success mb10 mr10' %>
        </div>
      </div>
    </div>

<% end %>
